(() => {
 const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];let social=null,client=null,session=null,all=[],filter='all',channel=null;const grid=$('[data-tournament-grid]'),listView=$('[data-tournament-list-view]'),detail=$('[data-tournament-detail]');const toast=(m,k='info')=>social?.toast?.(m,k);const dt=v=>{const d=new Date(v);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('pt-BR',{dateStyle:'medium',timeStyle:'short'}).format(d)};const statusLabel={open:'Inscrições abertas',scheduled:'Próximo',in_progress:'Em andamento',finished:'Finalizado',cancelled:'Cancelado',draft:'Rascunho'};
 function card(t){const a=document.createElement('article');a.className='t-card';const cover=document.createElement('div');cover.className='t-card-cover';if(t.cover_url){const img=document.createElement('img');img.src=t.cover_url;img.alt='';img.onerror=()=>{cover.innerHTML='<span>🏆</span>';};cover.append(img)}else cover.innerHTML='<span>🏆</span>';const copy=document.createElement('div');copy.className='t-card-copy';const st=document.createElement('span');st.className='t-card-status '+t.status;st.textContent=statusLabel[t.status]||t.status;const h=document.createElement('h3');h.textContent=t.name;const p=document.createElement('p');p.textContent=t.description||'Torneio da comunidade Ítalo Football.';const meta=document.createElement('div');meta.className='t-card-meta';[['📅',dt(t.starts_at)],['🎮',t.platform],['👥',`${t.participant_count||0}/${t.max_participants}`]].forEach(([i,x])=>{const s=document.createElement('span');s.textContent=`${i} ${x}`;meta.append(s)});const act=document.createElement('div');act.className='t-card-actions';const link=document.createElement('a');link.className='social-btn primary';link.href=`torneios.html?t=${encodeURIComponent(t.slug)}`;link.textContent='Visualizar';act.append(link);copy.append(st,h,p,meta,act);a.append(cover,copy);return a}
 async function load(){const t=await client.from('italo_tournaments').select('id,slug,name,description,cover_url,starts_at,registration_deadline,max_participants,rules,platform,format,tournament_type,status,prize,champion_user_id,created_at').neq('status','draft').order('starts_at',{ascending:true});if(t.error){grid.innerHTML='<div class="t-empty">Não foi possível carregar os torneios.</div>';return}const ids=(t.data||[]).map(x=>x.id);let counts=[];if(session?.user&&ids.length){const p=await client.from('italo_tournament_participants').select('tournament_id,status').in('tournament_id',ids);counts=p.data||[]}all=(t.data||[]).map(x=>({...x,participant_count:counts.filter(p=>p.tournament_id===x.id&&p.status==='registered').length}));renderList()}
 function renderList(){grid.replaceChildren();const rows=filter==='all'?all:all.filter(x=>x.status===filter);if(!rows.length){grid.innerHTML='<div class="t-empty">Nenhum torneio nesta categoria.</div>';return}rows.forEach(x=>grid.append(card(x)))}
 async function profileMap(ids){ids=[...new Set(ids.filter(Boolean))];if(!ids.length)return new Map();const r=await client.from('italo_profiles').select('user_id,display_name,username,avatar_url,role_badge,is_channel_member').in('user_id',ids);return new Map((r.data||[]).map(x=>[x.user_id,x]))}
 function badge(p){if(p?.role_badge==='owner'){const b=document.createElement('span');b.className='role-badge owner';b.textContent='OWNER';return b}if(p?.role_badge==='admin'){const b=document.createElement('span');b.className='role-badge admin';b.textContent='ADM';return b}if(p?.is_channel_member){const b=document.createElement('span');b.className='member-exclusive-badge';b.textContent='★ Membro Exclusivo';return b}return null}
 async function renderDetail(slug,id=null){listView.hidden=true;detail.hidden=false;detail.innerHTML='<div class="t-loading">Carregando torneio…</div>';let tq=client.from('italo_tournaments').select('*');tq=id?tq.eq('id',id):tq.eq('slug',slug);const tr=await tq.maybeSingle();if(tr.error||!tr.data){detail.innerHTML='<div class="t-empty">Torneio não encontrado.</div>';return}const t=tr.data;let participants=[],matches=[];if(session?.user){const [p,m]=await Promise.all([client.from('italo_tournament_participants').select('*').eq('tournament_id',t.id).order('joined_at'),client.from('italo_tournament_matches').select('*').eq('tournament_id',t.id).order('round_no').order('match_no')]);participants=p.data||[];matches=m.data||[]}const ids=[...participants.map(x=>x.user_id),...matches.flatMap(x=>[x.participant1_id,x.participant2_id,x.winner_id]),t.champion_user_id];const profiles=await profileMap(ids);detail.replaceChildren();const back=document.createElement('a');back.href='torneios.html';back.className='social-btn secondary t-back';back.textContent='← Todos os torneios';detail.append(back);const hero=document.createElement('section');hero.className='t-detail-hero';hero.innerHTML=`<span class="t-card-status ${t.status}">${statusLabel[t.status]||t.status}</span><h2></h2><p></p>`;hero.querySelector('h2').textContent=t.name;hero.querySelector('p').textContent=t.description||'';const actions=document.createElement('div');actions.className='t-detail-actions';if(session?.user){const mine=participants.find(x=>x.user_id===session.user.id);if(t.status==='open'&&Date.now()<=new Date(t.registration_deadline).getTime()&&mine?.status!=='registered'){const join=document.createElement('button');join.className='social-btn primary';join.textContent='Inscrever-se';join.addEventListener('click',()=>joinTournament(t.id,join));actions.append(join)}if(mine?.status==='registered'&&['open','scheduled'].includes(t.status)){const leave=document.createElement('button');leave.className='social-btn secondary';leave.textContent='Cancelar inscrição';leave.addEventListener('click',()=>leaveTournament(t.id,leave));actions.append(leave)}}else{const login=document.createElement('a');login.href='perfil.html';login.className='social-btn primary';login.textContent='Entrar para participar';actions.append(login)}if(['admin','owner'].includes(social?.state?.access?.role)){const adm=document.createElement('a');adm.href='admin.html#torneios';adm.className='social-btn secondary';adm.textContent='Gerenciar no ADM';actions.append(adm)}hero.append(actions);const info=document.createElement('div');info.className='t-detail-info';[['Data',dt(t.starts_at)],['Plataforma',t.platform],['Participantes',`${participants.filter(x=>x.status==='registered'||x.status==='champion').length}/${t.max_participants}`],['Formato',t.format==='knockout'?'Mata-mata':'Grupos + mata-mata']].forEach(([k,v])=>{const x=document.createElement('div');x.className='t-stat';x.innerHTML='<small></small><strong></strong>';x.querySelector('small').textContent=k;x.querySelector('strong').textContent=v;info.append(x)});hero.append(info);detail.append(hero);if(t.champion_user_id){const p=profiles.get(t.champion_user_id);const c=document.createElement('div');c.className='t-champion';c.innerHTML='<b>🏆</b><div><small>CAMPEÃO</small><strong></strong></div>';c.querySelector('strong').textContent=p?.display_name||'Campeão';detail.append(c)}const part=section('Participantes');const plist=document.createElement('div');plist.className='participant-list';if(!session?.user){plist.innerHTML='<div class="t-empty">Entre para visualizar a lista de participantes.</div>'}else{const visible=participants.filter(x=>x.status!=='removed');if(!visible.length)plist.innerHTML='<div class="t-empty">Nenhum participante inscrito.</div>';visible.forEach(x=>{const p=profiles.get(x.user_id);const row=document.createElement('article');row.className='participant';const img=document.createElement('img');img.src=p?.avatar_url||'assets/avatar.jpg';img.alt='';const cp=document.createElement('div');cp.className='participant-copy';const a=document.createElement('a');a.href=p?.username?`perfil.html?u=${encodeURIComponent(p.username)}`:'perfil.html';a.textContent=p?.display_name||'Jogador';cp.append(a);const b=badge(p);if(b)cp.append(b);const s=document.createElement('small');s.textContent=p?.username?`@${p.username} • ${x.status}`:x.status;cp.append(s);row.append(img,cp);plist.append(row)})}part.querySelector('.t-section-body').append(plist);detail.append(part);const br=section('Chaveamento');const body=br.querySelector('.t-section-body');if(!session?.user)body.innerHTML='<div class="t-empty">Entre para visualizar o chaveamento.</div>';else if(!matches.length)body.innerHTML='<div class="t-empty">O chaveamento será exibido quando o torneio começar.</div>';else body.append(makeBracket(matches,profiles));detail.append(br);if(t.rules){const rules=section('Regras');const r=document.createElement('div');r.className='t-rules';r.textContent=t.rules;rules.querySelector('.t-section-body').append(r);detail.append(rules)}}
 function section(title){const s=document.createElement('section');s.className='t-section';s.innerHTML='<header class="t-section-head"><h3></h3></header><div class="t-section-body"></div>';s.querySelector('h3').textContent=title;return s}
 function makeBracket(matches,profiles){const wrap=document.createElement('div');wrap.className='bracket-wrap';const br=document.createElement('div');br.className='bracket';const rounds=[...new Set(matches.map(x=>x.round_no))];rounds.forEach(r=>{const col=document.createElement('div');col.className='bracket-round';const h=document.createElement('h4');h.textContent=r===rounds.length?'Final':`Rodada ${r}`;col.append(h);matches.filter(x=>x.round_no===r).forEach(m=>{const box=document.createElement('article');box.className='match';[[m.participant1_id,m.score1],[m.participant2_id,m.score2]].forEach(([id,score])=>{const p=profiles.get(id);const row=document.createElement('div');row.className='match-player'+(m.winner_id===id?' winner':'');const n=document.createElement('span');n.textContent=id?(p?.display_name||'Jogador'):'A definir';const sc=document.createElement('b');sc.textContent=score==null?'—':String(score);row.append(n,sc);box.append(row)});col.append(box)});br.append(col)});wrap.append(br);return wrap}
 async function joinTournament(id,b){b.disabled=true;const r=await client.rpc('italo_join_tournament',{tournament_id:id});b.disabled=false;if(r.error){toast(r.error.message,'error');return}toast('Inscrição confirmada.','success');location.reload()}
 async function leaveTournament(id,b){if(!confirm('Cancelar sua inscrição neste torneio?'))return;b.disabled=true;const r=await client.rpc('italo_leave_tournament',{tournament_id:id});b.disabled=false;if(r.error){toast(r.error.message,'error');return}toast('Inscrição cancelada.','success');location.reload()}
 function currentDetail(){const params=new URLSearchParams(location.search);return{slug:params.get('t'),id:params.get('id')}}
 function startRealtime(){if(!session?.user)return;channel=client.channel('italo-tournaments-ui').on('postgres_changes',{event:'*',schema:'public',table:'italo_tournaments'},()=>{const x=currentDetail();if(x.slug||x.id)renderDetail(x.slug,x.id);else load()}).on('postgres_changes',{event:'*',schema:'public',table:'italo_tournament_participants'},()=>{const x=currentDetail();if(x.slug||x.id)renderDetail(x.slug,x.id);else load()}).on('postgres_changes',{event:'*',schema:'public',table:'italo_tournament_matches'},()=>{const x=currentDetail();if(x.slug||x.id)renderDetail(x.slug,x.id)}).subscribe()}
 function bind(){$$('[data-tournament-filter] button').forEach(b=>b.addEventListener('click',()=>{$$('[data-tournament-filter] button').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;renderList()}))}
 async function init(s){social=s;client=s.client;session=s.state.session;bind();const x=currentDetail();if(x.slug||x.id)await renderDetail(x.slug,x.id);else await load();startRealtime()}
 document.addEventListener('italo:social-ready',e=>init(e.detail),{once:true});if(window.ITALO_SOCIAL)init(window.ITALO_SOCIAL);
})();
