(() => {
  const client=window.ITALO_SUPABASE;if(!client)return;const $=(s,r=document)=>r.querySelector(s);const host=$('[data-video-grid]'),state=$('[data-video-state]'),search=$('[data-video-search]');if(!host)return;let videos=[];
  const setState=(t,k='')=>{state.textContent=t;state.dataset.state=k;state.hidden=!t};const norm=v=>String(v||'').toLocaleLowerCase('pt-BR').normalize('NFD').replace(/[\u0300-\u036f]/g,'');
  function card(v){const a=document.createElement('a');a.className='video-real-card';a.href=`https://www.youtube.com/watch?v=${encodeURIComponent(v.video_id)}`;a.target='_blank';a.rel='noopener noreferrer';a.dataset.videoId=v.video_id;const media=document.createElement('div');media.className='video-real-thumb';const img=document.createElement('img');img.src=`https://i.ytimg.com/vi/${v.video_id}/maxresdefault.jpg`;img.alt=v.title;img.loading='lazy';img.decoding='async';img.onerror=()=>{img.onerror=null;img.src=`https://i.ytimg.com/vi/${v.video_id}/hqdefault.jpg`};const play=document.createElement('span');play.className='video-real-play';play.textContent='▶';media.append(img,play);const copy=document.createElement('div');copy.className='video-real-copy';const source=document.createElement('span');source.className='video-real-source';source.textContent='YouTube oficial';const title=document.createElement('strong');title.textContent=v.title;title.dataset.videoTitle='';const meta=document.createElement('small');meta.textContent='Abrir vídeo no canal';meta.dataset.videoMeta='';copy.append(source,title,meta);a.append(media,copy);hydrateOembed(v.video_id,title,meta,img);return a}
  async function hydrateOembed(id,title,meta,img){try{const r=await fetch(`https://www.youtube.com/oembed?url=${encodeURIComponent(`https://www.youtube.com/watch?v=${id}`)}&format=json`,{credentials:'omit'});if(!r.ok)return;const data=await r.json();if(data?.title){title.textContent=data.title;img.alt=data.title}if(data?.author_name)meta.textContent=`Canal: ${data.author_name}`;}catch{/* A ausência de oEmbed não impede o link/thumbnail reais. */}}
  function render(){const q=norm(search?.value);const rows=videos.filter(v=>!q||norm(v.title).includes(q));host.replaceChildren();if(!rows.length){setState('Nenhum vídeo corresponde à busca.','empty');return}setState(`${rows.length} vídeo${rows.length===1?'':'s'} real${rows.length===1?'':'is'} do canal.`,'success');rows.forEach(v=>host.append(card(v)))}
  async function init(){setState('Carregando vídeos reais…','loading');const r=await client.from('italo_videos').select('video_id,title,channel_url,featured,sort_order').eq('is_active',true).order('sort_order');if(r.error){setState('Não foi possível carregar a seleção de vídeos.','error');return}videos=r.data||[];render();search?.addEventListener('input',render)}init();
})();
